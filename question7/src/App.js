import logo from './logo.svg';
import './App.css';
// Create an array of objects representing students in a class. Each object has the
//  following properties: id, name, grade, and attendance. Write a React component where you can
//   create an array of students and generate an ordered list of students, where each list item 
//   displays the student's name, grade, and attendance.
function App() {
  return (
    <>
     
    </>
  );
}

export default App;
